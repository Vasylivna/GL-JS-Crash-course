const MATH_PI = 3.14;
const radius = 10;
const formula = "circumference";

//ternary
const result = formula === "circumference"
    ?MATH_PI * (2 * radius)
    :MATH_PI * (radius2 * radius);

console.log(result);
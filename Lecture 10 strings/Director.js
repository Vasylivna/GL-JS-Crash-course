const director = {
  firstName: 'Steven',
  lastName: 'Spielberg',
  movies: 57,
};

const info = 'As a Director ' + user.firstName
	+ ' ' + user.lastName + ' created ' + 
    + director.movies + ' movies';

console.log(info);
